import { NumberOfElementsToArrayPipe } from './number-of-elements-to-array.pipe';

describe('NumberOfElementsToArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberOfElementsToArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
